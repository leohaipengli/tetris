(not important now since we don't need grid lines now)1. use two buffers:
    one for grid (static)
    one for bricks (dynamic)

2. use different shader:
    dark color for grids
    differeny color for different bricks